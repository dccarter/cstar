//import { Vector } from "stdlib/vector.cxy"

class Demo {
    func `init`() {
        stdout << "Initializing....\n"
    }

    func `deinit`() {
        stdout << "De-initializing....\n"
    }
}

pub func main() {
    var x = make[Demo]();
    ref(x)
    deallocate(x)
}
