import "add.h" as Add
import "stdio.h" as stdio

struct Say[T] {
    func say[U](t: T, u: U) {
        println(t, u)
    }
}

pub func main() {
    var x = Say[i32]{};
    info!("{t}", typeof!(x))
    x.say(10, "Hello")
}
