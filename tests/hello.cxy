import { HashMap } from "stdlib/map.cxy"
import { println } from "stdlib/io.cxy"
import { String } from "stdlib/str.cxy"

func main(args:[string]) {
    var names = HashMap[String, i32]();
    names.["age"] = 19;
    const age = names.["age"];
    println(names)
    println(age)
    var str = String();
    str = str.append("hello")
    str = str.append(" ")
    println(str)
}
