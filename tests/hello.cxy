import { println, fdRead, STDIN } from "stdlib/io.cxy"

async func get(url: string) => f"You said: ${url}"

func main(args: [string]) {
    var x: [u8, 128];
    fdRead(STDIN, unchecked!(x, #&void), 128);
}
