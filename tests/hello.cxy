import { HashMap, HashSet } from "stdlib/map.cxy"

pub func main(argc: i32, argv: &string) {
    var nums = HashSet[i32]();
    for (const i: 0..100) {
        nums.insert(<i32>i%10)
    }

    println(nums)
    println(nums.contains(1))
}
