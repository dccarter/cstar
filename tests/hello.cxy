import { println } from "stdlib/io.cxy"
import { Vector } from "stdlib/vector.cxy"
//
interface MemoryAllocator {
    func allocate(size: u64): &void
    func free(ptr: &void) : void
}

class PoolAllocator ::MemoryAllocator {
    func `init`(){}
    func allocate(size: u64): &void => null
    func free(ptr: &void) : void => {}
}

func main(args: [string]) {
    hello(new[PoolAllocator](pool, ...))
}
