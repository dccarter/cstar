import { println } from "stdlib/io.cxy"


func say(name: string, fn: func(msg: String) -> void) {
    fn(f"Hello ${name}")
}

func main(args: const [string]) {
    var x = (msg: String) => println(msg);
}
