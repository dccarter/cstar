import "add.h" as Add
import "stdio.h" as stdio

pub func main() {
    defer println("Defer1")
    println("Hello ")
    var x = true;
    while(x) {
        defer println("> Defer2")
        x  = false
        if (x) {
            defer println(">> Defer3")
            break
        }
        else if (true) {
            return 0
        }
    }
}
