//import "stdlib/path.cxy"

struct User {
    - name: String;
    - age = 35:i32;
    func `init`(@unused name: String) {
        this.name = &&name
    }
}

exception IOError(msg: String = String("Input/Output error")) => msg.str()
class Hello { func `init`(){} }

func say(x: i32): !void {
    if (x == 0) {
        raise IOError()
    }
    return;
    println( "Hello World" )
}

func main() {
}
