import { Vector } from "stdlib/vector.cxy"

pub func main() {
    var vec = Vector[i32]();

    for (const i: 0..10) {
        vec.push(i*3)
    }

    for (const m,_: vec) {
        stdout << m
    }
}
