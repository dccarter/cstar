import { println } from "stdlib/io.cxy"
import { StringBuilder } from "stdlib/strlib.cxy"
import { Vector } from "stdlib/vector.cxy"

enum  Chars { Z = 6, A = 0, M = 3, K = 4, L, B, E = 2}

func hello(a: Chars) : bool {
    switch(a) {
    case Chars.A: return true;
    case Chars.Z: break;
    default:
        return false;
    }
}

func oops(i: i32) {
    if (i == 0)
        return true;
}

func main(args: [string]) {
    var vec1 = Vector[i32](10);
    var vec2 = Vector[i32](10);
    var sb = StringBuilder();
    sb.appendString("Hello World! ")
    sb.appendInt(10)
    println(f"${Chars.A}")
    println(sb.toString())
}

