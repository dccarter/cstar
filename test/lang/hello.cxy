@grpc
struct BankAccount {
    type Command = Deposit|Withdraw

    - balance: f32 = 0.0

    @grpc(enable: true)
    func deposit(amount: f32) {
        this.balance += amount;
    }
}

func main(args: string) {
    var account = new BankAccount();
    account.deposit(3000);
}